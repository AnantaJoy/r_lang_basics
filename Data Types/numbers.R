# Numeric in R

weight <- 83.7
class(weight)

age <- 53
class(age)


# Integer: Numeric without decimal
weight <- 83L
class(weight)


# Complex: number is written with `i` as imaginary part
current <- 4 + 5i
class(current)